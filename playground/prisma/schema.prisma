
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model TestLog {
  id        Int      @id @default(autoincrement())
  message   String
  createdAt DateTime @default(now())
}
    